# EXP-NNN · <实验标题>

| 字段 | 值 |
|---|---|
| 日期 | YYYY-MM-DD |
| 环境 | ENV-x（sha, vllm 版本） |
| 状态 | 完成 / 部分完成 / 作废（写明原因） |
| 关联清单项 | R?-? / B? / C? / D? |

## 1. 目的与假设
一句话目的；若是验证性实验，写出可证伪的假设。

## 2. 环境与配置
完整启动命令（服务端 + 客户端 + 环境变量），硬件占用（哪张卡），
与其他实验共享/复用的状态（如服务是否复用、缓存冷热）。

## 3. 步骤
可复现的命令序列（或指向脚本 + 参数）。

## 4. 原始数据
文件指针列表（每个文件首行应有 provenance）；
若有任何数字只来自终端输出而无 raw 文件，**必须在此注明**并标注证据等级。

## 5. 结果
关键数字表。只放本实验直接产出的测量值，不放推论。

## 6. 分析与结论
对数字的解释、与假设的对照、机理判断（标明哪些是实测、哪些是推断）。

## 7. 异常、偏差与开放问题
过程中的意外、协议偏离（如改了参数）、未解释的现象；开放问题写去向（谁接、哪个实验跟进）。

## 8. 下游影响
对 gate/红线/SLO/工装/简历句 的影响；触发的规则或工装变更。
