# 零基础教学手册：把今天的实验变成你自己的知识

> ⚠ 本文数字为 2026-08-21 Day 0 快照；最新状态与数字以 HANDOFF.md 与 LEDGER.md 证据台账为准（8/23 已新增 EXP-013~017）。

> 目标读者：项目所有者本人（自述"现在啥也不懂"）。读完你应该能： ①用自己的话讲清每个概念；②背出核心数字并知道它从哪来；③应对面试追问。学习顺序就按本文顺序。每个概念都用**我们自己的实测数字**当例子——这是你和背书本的人的区别。

---

## 第一部分 · 概念速成（每个 ≤5 分钟）

### 1. LLM 推理的两个阶段：prefill 和 decode
- **prefill（预填充）**：把你的整段输入（prompt）一次性算完，生成第一个输出 token。计算量大（矩阵乘堆满 GPU），**计算受限**。
- **decode（解码）**：之后每次只生成 1 个 token。每生成一个 token 都要把整个模型权重从显存读一遍，**显存带宽受限**。
- **我们的数字**：7B 模型单张 4090，8192 token 的 prefill 要 ~925ms；decode 每 token 只要 ~16ms。**验证带宽约束**：7B 模型 BF16 权重 ≈15GB，4090 带宽 ~1008GB/s → 理论下限 15/1008 ≈ 14.9ms/token，我们实测 16ms——对上了，这就叫"decode 是权重带宽受限"。

### 2. TTFT / TPOT / E2E
- **TTFT**（Time To First Token）＝用户按下回车到第一个字出现，≈ 排队 + prefill。
- **TPOT**（Time Per Output Token）＝之后每个字的间隔，≈ decode 速度。
- 我们的无负载基线：TTFT 65/225/925ms（输入 512/2K/8K，注意近似线性——因为 prefill 计算量 ∝ 输入长度），TPOT 恒 16ms（和输入长度无关——因为 decode 只读权重和 KV）。

### 3. KV cache（一切 PD 故事的主角）
- 注意力机制需要"前文"的 Key/Value 张量；不想每步重算，就缓存下来 = KV cache。
- **算给你看**（Qwen2-7B）：28 层 × 4 个 KV 头 × 128 维 × K 和 V 2 份 × BF16 2 字节 = **57,344 字节/token ≈ 56KB/token**。8192 token 的请求 ⇒ 8192×57344 ≈ **470MB**。我们实测 PD 传输 469.8MB/请求——和公式分毫不差（面试杀手锏）。
- KV cache 按 **block**（我们这里 16 token 一块）管理，像内存分页。

### 4. Continuous batching（vLLM 为什么快）
- 不是"攒一批一起跑"，而是**每个 decode 步都动态进出请求**——新请求随时插进来，完成的随时退出。GPU 始终吃饱。
- 后果：负载越高单请求越慢（互相抢带宽），但总吞吐上升——这就是我们扫负载曲线的意义：**延迟 vs 吞吐的交换曲线**。

### 5. SLO 和 goodput（我们报告的"记分规则"）
- **SLO**：服务质量承诺，如"TTFT ≤ 891ms 且 TPOT ≤ 50ms"。
- **goodput**：每秒**达标**的请求数（不达标的白干）。比吞吐诚实——吞吐可以靠牺牲延迟刷高。
- 我们的 SLO 定义：TTFT ≤ 5×无负载基线（DistServe 的相对式定法，避免 512 和 8K 桶用同一个绝对值时信息量归零），TPOT ≤ 50ms（=20 token/s，约人类阅读速度 3 倍）。**为什么是 5×？**——附录 fig6 证明 0.5–4× 区间臂间排序不变，结论不依赖阈值（这是防"挑阈值"质疑的标准答案）。

### 6. 四种部署形态（我们的"四臂"）
1. **colocate（单卡混部）**：一张卡，P 和 D 都干。最简单，KV 不用搬。
2. **replica2（双副本）**：两张卡各跑一个完整实例，前面加轮询代理。卡之间 **零通信**。
3. **TP=2（张量并行）**：一个模型劈成两半分到两张卡，每层算完要 **allreduce**（两卡互相把部分和加起来）。每步都通信。
4. **PD 分离（1P1D）**：一张卡专门 prefill（P），一张专门 decode（D），P 算完把 **整段 KV cache 搬给 D**。动机：prefill 是重活，会卡住同卡上别人的 decode（干扰）；分开后互不打扰、还能独立扩容。**代价：搬 KV**。

### 7. 为什么我们的机器上 TP2 和 PD 都吃亏（本项目的灵魂）
- 4090 是消费卡：**驱动禁用 P2P**（两卡不能直接互读显存，topo 显示 GNS），数据要绕道 CPU 内存。我们实测三条路径：
  - NCCL allreduce（TP 用）：collective 带宽受限（实测值待复核，见 EXP-018）
  - NIXL KV 传输（PD 用）：**0.27 GB/s**（descriptor 16KB 碎片化小拷贝）
  - 对照：卡内带宽 924 GB/s——差 3 个数量级！
- **TP2 的账**：decode 时每卡只读半份权重 → TPOT 16→9.3ms（-42%）✓；但 prefill 的 allreduce 是大消息（28 层 × 58.7MB@8K = 1.64GB），受 collective 带宽约束要传近 1 秒 → prefill 零加速。批量化后 decode 优势也稀释 → 双卡只换 +13–19% 吞吐。
- **PD 的账**：8K 请求 KV=470MB ÷ 0.27GB/s ≈ **1.7 秒纯传输**，直接加在 TTFT 上（实测 PD TTFT 2719ms，其中传输 1730ms 占 64%）。饱和吞吐被传输带宽卡死： 0.27/0.47 ≈ 0.57 req/s，实测 0.54 ✓。
- **结论一句话**：并行/分离形态的收益，全部取决于互联带宽；互联差时， **不通信的并行（replica）是唯一免费午餐**。

### 8. NIXL 与 P2pNccl（S2 故事）
- 两代 KV 搬运机制。旧的 P2pNccl（0.17 时代）：P 和 D 各自拿 `request_id#层名` 当"接头暗号"——但 vLLM 会给每个实例的 request_id 加随机后缀（每实例不同！），暗号必然对不上 → D 端在一个**没有超时**的等待里挂死整个实例。修不动的原因是**协议里没有身份字段**（隐式契约）。
- 新的 NIXL（现在）：P 把自己的 `engine_id + request_id + block 列表 + 地址` **显式**交给代理转发给 D，D 直接按内存地址 RDMA 读——身份拆成三层（引擎/会话/内存寻址）显式映射。同样的随机后缀机制还在，NIXL 毫发无损。
- 面试金句："这是显式契约对隐式契约的胜利"；细节武器库： `pd_disagg/analysis/p2pnccl_bugs_id_chain.md`（背下 file：line 更狠： assert 在 connector：433，随机后缀在 input_processor.py：212，死等在 engine：317）。

### 9. 功率帽节流（我们的意外发现，面试讲"探案"）
- 现象：colocate 8K 的 TTFT 前 8 个请求 ~700ms，之后 ~925ms；replica2 却全程 ~715ms（轮转=每卡半负载）。
- 排查：nvidia-smi 采样 → 450W 功率墙（不是热，才 63°C），SM 频率 2820→2475。
- 教训：消费卡的基准必须声明功率工况；我们所有正式数据都在同热工况下测。

### 10. 前缀缓存污染（我们的另一个探案，方法论素养的展示）
- vLLM 默认开 prefix caching（相同开头的请求直接复用 KV）。基准工具用固定 seed 生成"随机"prompt → 重跑时 prompt 一模一样 → 缓存命中 → 数字虚好。更隐蔽：同 seed 下 512 的 prompt 恰是 2048 的前缀！
- 我们怎么抓的：两个独立计数器（传输字节 vs 按来源 token 计数）对账，缺口 1050 个 block，其中 511 块直接定罪到 bench 自带的 test 请求（源码 serve.py：824）。
- 修正：每个测量点唯一 seed（协议 v2），全部重跑。**面试点**：数据自洽性要靠交叉对账，不是靠相信工具。

---

## 第二部分 · 必背数字卡（12 张）

| # | 数字 | 是什么 | 出处 |
|---|---|---|---|
| 1 | collective 带宽（值待复核） | NCCL allreduce（TP 路径） | nccl-tests 实测，见 EXP-018 |
| 2 | 0.27 GB/s | NIXL KV 有效吞吐（PD 路径） | telemetry， 跨尺寸恒定 |
| 3 | 56 KB/token | Qwen2-7B 的 KV 大小 | 28 层×4 头×128×2×2B |
| 4 | 470 MB | 8K 请求的 KV 总量 | 实测 469.8MB 对账 |
| 5 | 16 ms | 单卡 decode TPOT | ≈15GB 权重/1008GB/s |
| 6 | 9.3 ms | TP2 的 TPOT（-42%） | 每卡半份权重 |
| 7 | 65/225/925 ms | 无负载 TTFT（512/2K/8K） | 近线性=计算主导 |
| 8 | 2719 ms | PD 的 8K TTFT（传输占 64%） | 分解对账 |
| 9 | 10.36 vs 15.58/12.31/7.84 | 512 桶四臂饱和 req/s | colo/repl/tp2/pd |
| 10 | 2× / +15% / 0.6× | replica/tp2/pd 相对单卡的吞吐 | 2K 桶 |
| 11 | +45% | 0.17.1→0.25.1 短请求饱和提升 | 延迟不变 |
| 12 | 4.62 ms | MoE-A2.7B 的 TPOT（未调优） | dense TP2 的 2.0× |

## 第三部分 · 面试问答预演（15 题）

1. **"介绍一下这个项目"（60 秒版）**： "我在两张 4090 上回答一个真实工程问题：有限预算下该选哪种 vLLM 部署形态。我先实测了机器的三条互联路径——allreduce 受 collective 带宽约束、KV 传输 0.27GB/s——然后跑了四种形态 × 三档输入 × 多档负载共 100+ 个带 gate 校验的测量点。结论是双副本近线性扩展、TP2 只值 +15%、PD 分离在这种互联下全负载段不可取——而且每个结论都能对账到硬件数字：比如 PD 的 8K 饱和吞吐 0.54 req/s，就等于 0.27GB/s 除以每请求 470MB 的 KV。"
2. **"凭什么说 KV 传输真的发生了？"** → gate 体系：failure_policy=fail、 NIXL bytes 增量 = 请求数×KV 量精确对账、双计数器互证、失败/过期计数=0，全部与数据同行存在 runs.jsonl。
3. **"为什么 TP2 的 prefill 没加速？"** → 大消息 allreduce（28 层×58.7MB@8K）受 collective 带宽约束 ≈0.9s，把计算减半的收益吃光。
4. **"为什么 PD 分离还有人用？"** → 它的主场是大集群+NVLink/IB：干扰隔离与 P/D 独立扩缩。我证明的是适用边界，不是否定架构（报告 §2.4 原话）。
5. **"SLO 为什么是 5× 和 50ms？"** → 相对式定法（DistServe）+ 敏感性附录 0.5–4× 排序稳定 + 预注册防事后挑阈值。
6. **"goodput 和吞吐有什么区别？"** → 见概念 5；顺带讲 colocate 512 桶超载后 goodput 从 8.57 崩到 5.96 而吞吐还在涨——这就是区别。
7. **"讲一个你踩过的坑"** → 前缀缓存污染探案（概念 10）或功率帽探案（概念 9），两个都是"异常→拆解→机理→修正协议"的完整链。
8. **"0.17 那两个 bug 是你发现的吗？"** → 不是，课程已记录；我做的是复现级源码定位与机理验证，并对照新架构验证了修复思路（措辞红线）。
9. **"NIXL 为什么对随机后缀免疫？"** → remote_request_id 是 P randomize 之后亲口告知的，不是 D 推导的（显式 vs 隐式契约）。
10. **"你的测量误差有多大？"** → 同条件复测 TTFT 差 <1%（65.4 vs 65.5）；已知噪声：客户端瞬断 ~3% 运行率（重跑处理）、功率工况（遥测在案）。
11. **"MoE 为什么 decode 快？"** → 只激活 2.7B 参数 → 权重读取少 → 4.62ms；但饱和吞吐没有等比提升（路由/分发开销），这正是 9 月要 nsys 分解的对象。
12. **"E=30，N=1408 是什么意思？"** → fused MoE Triton kernel 的调优 key：每 rank 专家数 E（60 专家 TP2+EP 后每卡 30）、FFN 中间维 N（1408）；社区没有 4090 的调优文件，运行时告警为证，这就是我的 PR 切入点。
13. **"为什么不做 Mooncake/LMCache 兜底？"** → LMCache 的 disagg 路径本身走 NIXL（不构成独立失败域），Mooncake 需要额外 RDMA 依赖（范围控制）。
14. **"如果给你 NVLink 的卡，结论会怎么变？"** → 三条互联数字全变 → TP2 的 allreduce 墙消失、PD 传输成本 /50+ → 边界重画；方法论不变（这题考的是你知道结论的适用条件）。
15. **"数据我能复现吗？"** → runs.jsonl 每行带完整 provenance + seed + 命令， figures 全部由脚本从 raw 重算；仓库结构讲一遍（LEDGER 台账/records/日记）。

## 第四部分 · 明天的学习路径（建议 4 小时）

1.（30min）读本文第一部分，每读完一个概念，合上文档用自己的话讲一遍。 2.（30min）读 [REPORT.md](pd_disagg/REPORT.md) 的一页结论 + 六张图，练习 "指着图讲故事"（每张图 30 秒）。 3.（60min）读 [LAB_JOURNAL.md](LAB_JOURNAL.md) §9–§12 两个探案 + [analysis/](pd_disagg/analysis/) 双 bug 分析，把 2 分钟口径念熟。 4.（30min）背数字卡（第二部分），用第三部分 Q1 做 3 遍自我陈述。 5.（60min）把第三部分 15 题全部口头过一遍，卡壳的回到对应文档补。 6.（30min）打开 RESUME_EVIDENCE.md，按你顾问的三条框架压缩简历句初稿。

有不懂的概念随时问我——我可以对着你的实验数据逐个拆解。
