# 怎么读这个项目(vllm/experiments · 推理部署选型与 MoE 优化)

> **上游**：跨仓的先后次序见 `Resume/STUDY_MAP.md`；本文只讲**进了这个仓之后怎么读**。
> **同级**：各仓根目录的同名文件结构一致，读熟一个就会读其余五个。
> **和另外两份文档怎么分工**：`HANDOFF.md` 是**给接手 agent** 的状态入口
> （git HEAD / 硬件占用 / 下一步），`STUDY_GUIDE.md` 是 Day 0 的**零基础概念速成**
> （已标注为快照，数字以 LEDGER 为准）。本文讲的是**已经建成之后怎么读**，三者不重复。

---

## 总纲:这是唯一一个"你不写代码,而是选型"的仓

其余五个仓的产出是"我实现了 X"；**本仓的产出是"在这套硬件上,应该选哪条路"**。

被测对象是现成的 vLLM，变量是部署形态（colocate / PD 分离 / TP / 副本）、
模型形态（dense / MoE / 量化）与硬件约束（2×RTX 4090，**无 NVLink、P2P 驱动禁用**）。

本仓最核心的一句话不是任何一个数字，而是那条硬件约束 ——
**四条关键发现里有三条直接由它推出**：
「没有 NVLink 时，最优互联策略是避免互联」「消费卡的功率帽是隐藏变量」
「Ada 上的量化选型由硬件分派路径决定」。

读这个仓，如果记住了结论却没记住硬件前提，那些结论一换机器就全错。
**在这个仓里，限定条件比结论更重要。**

第二条：本仓是**唯一一个有 `HANDOFF.md` 的仓**（多 agent 接力），
所以状态的权威链路和别处不同：状态入口（状态三行）→ 状态与措辞的唯一权威 → 对外门面。

---

## 零、第一次进仓:按顺序打开(90 分钟)

| 步 | 打开什么 | 只看什么 | 时间 |
|---|---|---|---|
| 1 | `HANDOFF.md` | §0「这是什么项目」+ 状态三行 | 10 min |
| 2 | `README.md` | 「概述」+「性能结果」表 +「关键发现」四条 | 20 min |
| 3 | `docs/theory/02_pd_kv_path.md` | 全文(五节速查) | 15 min |
| 4 | `pd_disagg/REPORT.md` | 结论段与四臂矩阵表 | 20 min |
| 5 | `records/EXP-013_ext1_request_level_kv_attribution.md` | §1 假设 + §5 结果 + §6 分析 | 20 min |
| 6 | 白板自测 | 说清「为什么在这套硬件上 PD 分离反而最差」 | 5 min |

**第 5 步是这个仓的钥匙。** EXT-1 是约 16 行打在 vLLM NIXL connector 上的
可观测性 patch，把「KV 传输占 TTFT 多少」从**分量对账的推断**升级成
**逐请求的因果测量**。

它展示的能力最难被替代：
**现成系统不给你想要的指标时，你能不能最小侵入地把它测出来。**
不是"我会用 profiler"，是"没有指标我就造一个"。

---

## 一、完整阅读顺序(精确到文件)

分三段，按**问题域**排，不按编号。

### 第一段 · 部署选型(EXP-001~008、013)

| # | 主题 | records 入口 | 目录 | 走完能答 |
|---|---|---|---|---|
| 1 | 硬件基线 | `EXP-002_hardware_baseline` | `pd_disagg/hw/` | 无 NVLink / P2P 禁用意味着什么 |
| 2 | 工具链 | `EXP-003_profiling_tooling` | `pd_disagg/profiling/` | nsys 看 vLLM decode 为什么必须 `--cuda-graph-trace=node` |
| 3 | **四臂矩阵** | `EXP-004_b1_colocate_attribution_slo` → `EXP-007_b1_sweep_campaign` | `pd_disagg/matrix/` | 四种部署形态怎么同协议比 |
| 4 | 功率帽 | `EXP-005_replica2_tp2_powercap` | `pd_disagg/results/` | 消费卡的隐藏变量怎么暴露 |
| 5 | PD 分离归因 | `EXP-006_pd1p1d_probe_attribution` | `pd_disagg/analysis/` | 为什么 PD 在这套硬件上最差 |
| 6 | **因果测量** | `EXP-013_ext1_request_level_kv_attribution` | `pd_disagg/ext1/` | 16 行 patch 怎么把推断升级为测量 |

### 第二段 · MoE(EXP-009、014~017)

| # | 主题 | records 入口 | 目录 | 走完能答 |
|---|---|---|---|---|
| 7 | MoE bringup | `EXP-009_c1_moe_bringup` | `moe_perf/` | MoE 的 decode 优势从哪来 |
| 8 | **kernel 分解** | `EXP-014_d1_moe_kernel_decomposition` | `moe_perf/d1_*` | MoE 前向的时间花在哪几个 kernel |
| 9 | config 调优 | `EXP-015_d2_moe_config_tuning` | `moe_configs/` | 调优产物怎么落地成 config |
| 10 | EPLB gate | `EXP-017_d5_eplb_gate` | — | 专家负载均衡的判据 |

### 第三段 · 量化选型(EXP-010、016)

| # | 主题 | records 入口 | 走完能答 |
|---|---|---|---|
| 11 | W4A16 bringup | `EXP-010_c3_w4a16_bringup` | 消费卡上 W4A16 怎么起 |
| 12 | **FP8 vs W4A16** | `EXP-016_d4_fp8_vs_w4a16` | 为什么"Ada 上的量化选型由硬件分派路径决定" |

配套讲义：`docs/lectures/01_deployment_arms.md`（部署四臂）与
`02_pd_attribution.md`（PD 归因）。

### 三个里程碑

- 走完**第一段**：能讲清"在给定硬件上做部署选型"的完整方法（四臂同协议 + 归因）。
- 走完 **#6（EXT-1）**：能讲清"指标不存在时怎么造一个"。
- 走完**第二、三段**：能讲清 MoE 与量化在消费卡上的选型依据。

---

## 二、文档分层:按目的进

本仓文件最多，层级也最容易迷路。**先记住权威链路**：

```
HANDOFF.md(状态三行,给接手者)
  → LEDGER.md(台账:状态/措辞/待办的唯一权威)
    → README.md(对外门面:结果与发现)
      → records/(证据) / docs/(原理与讲稿) / pd_disagg|moe_perf(数据与脚本)
```

| 你的目的 | 进哪一层 | 时间 |
|---|---|---|
| 现在是什么状态、下一步做什么 | `HANDOFF.md` §5 | 5 min |
| 项目全貌 | `SUMMARY.md`（注：其"还欠的事"已过时，以状态入口 §5 为准） | 15 min |
| 结论与数字 | `README.md` 性能结果 + 关键发现 | 20 min |
| 措辞能不能这么说 | `LEDGER.md` 的措辞表 | 5 min |
| **先看总览**（原理 + 数据 + 分析方法 + 140 道分类面试题一体） | `docs/TECH_DOC_vllm_engineering.md` | 30 min 建全貌 / 2 h 过面试题 |
| 某个机制的一页纸速查 | `docs/theory/01_moe_dispatch_chain.md` / `02_pd_kv_path.md` | 15 min/篇 |
| 机制要真的学会 | `docs/lectures/` 的**对应小节**(两篇) | 30-60 min |
| 查数字怎么来的、可不可信 | `records/EXP-0NN` 的 §2 / §5 / **§7** | 10 min |
| 练面试怎么讲 | `docs/talk/TALK.md` + `whiteboard/` | 20 min |
| 简历句与证据 | `RESUME_EVIDENCE.md` | — |
| 部署选型的完整报告 | `pd_disagg/REPORT.md` | 20 min |
| 清单进度 | `CHECKLIST_v3.md` / `WEEKLY.md` | 按需 |

**本仓特有的两条纪律**：

1. **`SUMMARY.md` 与 `STUDY_GUIDE.md` 都是快照，已标注为部分过时。**
   读它们可以，但任何"当前状态"以状态入口 §5 与措辞表为准。
   这是多 agent 接力仓的典型问题 —— **状态会过期，结论不会**。
2. **写任何对外文字之前，先读 `LEDGER.md` 的措辞表。**
   本仓有若干结论受硬件条件限定，漏掉限定就是过度宣称。

---

## 三、代码怎么看:读的是脚本与 patch,不是实现

### 读法一 · 先读实验矩阵的定义,再读脚本

`pd_disagg/matrix/` 与 `EXPERIMENT_PLAN.md` 定义了四臂是什么、什么被固定。
**先读矩阵定义，再读 `pd_disagg/scripts/` 下的执行脚本** ——
否则看到的只是一堆启动命令，看不出实验设计。

### 读法二 · EXT-1 的 patch 要逐行读,它是本仓最值钱的 16 行

`pd_disagg/ext1/` 下是打在 vLLM NIXL connector 上的可观测性补丁，
逐行带 `# EXT1` 标记、原件备份可还原。读它时问三个问题：

1. **它在哪里插桩？**（D 端 connector 首见请求处记双时钟起点）
2. **为什么是这两个点？**（因为要闭合"传输开始"与"传输完成"之间的因果链）
3. **为什么不能用现成指标？**（现成的只能做分量对账，拿 colocate 无负载 TTFT 近似 P 段）

**最小侵入 + 可还原 + 逐行标记**，这三条是给别人的系统打补丁的标准做法。

### 读法三 · MoE 分解看 `moe_perf/d1_*`

`d1_kernels.py` / `d1_analyze.py` / `d1_nsys.sh` 是一组：
采集 → 分解 → 归因。读的时候注意 **nsys 采 vLLM decode 必须
`--cuda-graph-trace=node`**，否则 CUDA graphs 里的 kernel 全部不可见，
首采会得到一张 prefill 混样的假分解表（这是本仓的一次真实事故）。

### 读法四 · 数据目录按实验编号找,不要通目录浏览

`pd_disagg/` 与 `moe_perf/` 下文件很多。正确的进法是：
**先从 records 的 §4「原始数据」拿到路径，再进目录** ——
反过来从目录名猜属于哪个实验会很痛苦。

---

## 四、自测验收

不看文档，在白纸上做：

| 里程碑 | 验收动作 |
|---|---|
| 第一段后 | 画四臂矩阵，说清每臂的变量与固定项；讲清"没有 NVLink 时最优策略是避免互联"的机制 |
| EXT-1 后 | 讲清分量对账与因果测量的差别，以及那 16 行插在哪、为什么 |
| 第二段后 | 讲清 MoE decode 优势的来源（激活参数量），以及它随 batch 递减直至反转的原因 |
| 第三段后 | 讲清 Ada 上 FP8 与 W4A16 的选型依据（硬件分派路径） |

面试前一晚：`docs/talk/TALK.md` + `whiteboard/` + **`LEDGER.md` 的措辞表**
（本仓的限定条件最多，最容易说过头）。

---

## 五、当前缺口

1. **`SUMMARY.md` 的"还欠的事"与 `STUDY_GUIDE.md` 的数字均为快照**，已在文首标注，
   以状态入口 §5 / 措辞表为准。
2. **`docs/lectures/01_deployment_arms.md` 含一处日期残留**，不符合对外文档不含日期的约定
   （讲义校验器会报 BANNED），待清理。
3. **config PR 已提交（#54372，OPEN 未合并）**（分支已备好、PR_DRAFT 六件套已回填），属用户侧动作。
4. 记录到 `EXP-029` 为止（29 份八节记录 + TEMPLATE），不是编号跳空。
